<%= form_for(@vote_schedule) do |f| %>
  <% if @vote_schedule.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@vote_schedule.errors.count, "error") %> prohibited this vote_schedule from being saved:</h2>

      <ul>
      <% @vote_schedule.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :date_from %><br>
    <%= f.datetime_select :date_from %>
  </div>
  <div class="field">
    <%= f.label :date_to %><br>
    <%= f.datetime_select :date_to %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
